# Issue #012: /mem-search Skill 구현

> Phase 2: Hook Layer | 의존성: #007

## 배경

사용자가 명시적으로 메모리를 검색할 수 있는 skill이 필요합니다.
Progressive Disclosure를 지원하여 토큰 효율적인 검색을 제공합니다.

## 작업 내용

1. **Skill 정의** (`src/skills/mem-search.ts`)
   ```typescript
   interface MemSearchSkill {
     name: '/mem-search';
     execute(args: MemSearchArgs): Promise<string>;
   }

   interface MemSearchArgs {
     query: string;
     layer?: 1 | 2 | 3;
     since?: string;      // "7d", "30d", "2024-01-01"
     limit?: number;
     type?: string;       // "error", "success", "bash"
   }
   ```

2. **명령어 파싱**
   ```
   /mem-search "JWT authentication"
   /mem-search --layer 3 obs-a1b2
   /mem-search "database" --since 7d --type error
   ```

3. **출력 형식**
   - Layer 1 (기본):
     ```
     🔍 검색 결과: "JWT" (5건)

     ┌────────────┬───────┬─────────────────────────┐
     │ ID         │ 점수  │ 요약                    │
     ├────────────┼───────┼─────────────────────────┤
     │ obs-a1b2   │ 0.95  │ JWT 미들웨어 구현       │
     └────────────┴───────┴─────────────────────────┘
     ```

   - Layer 3:
     ```
     📄 obs-a1b2 상세

     세션: 2024-01-15 (JWT 인증 구현)
     유형: tool_use
     내용:
       auth.ts에 JWT 검증 미들웨어 추가...
     ```

4. **에러 처리**
   - 잘못된 인자 시 사용법 안내
   - 결과 없을 때 메시지

## 인수 조건

- [x] 키워드 검색 동작
- [x] --layer 옵션 동작
- [x] --since 옵션 동작
- [x] --type 옵션 동작
- [x] --limit 옵션 동작
- [x] ID로 단일 조회 동작
- [x] 테이블 형식 출력

## Evidence

[완료 증빙](./evidence.md)

## 검증 명령

```bash
bun test src/skills/__tests__/mem-search.test.ts

# 테스트 케이스
# - 기본 검색
# - Layer 3 상세 조회
# - 필터 조합
# - 결과 없음 처리
# - 잘못된 인자 처리
```
